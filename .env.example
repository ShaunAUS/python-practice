# 설정 템플릿 — 복사해서 .env 로 사용: cp .env.example .env
# Java application.yml 의 키 목록 역할. 값은 환경/팀에 맞게 채움.
# 우선순위: 환경변수 > .env > app/config.py 기본값

# DB 연결 (Spring spring.datasource.url)
APP_DATABASE_URL=sqlite:///./board.db

# SQL 로그 출력 (Spring spring.jpa.show-sql)
APP_SQL_ECHO=false
